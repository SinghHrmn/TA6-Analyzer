# Python dependencies (installed automatically by `pip install -e .`).
# Pinned to the exact versions this project was tested against (dissertation
# audit Goal C3, 13 Aug 2026) via `pip freeze` in a clean environment, not
# arbitrary minimums -- a "pip install -e ." on a fresh clone should resolve
# to precisely these versions, so the headline results (Chapter 6) reproduce.
pypdf==6.13.1
reportlab==4.5.1
img2pdf==0.6.3
pillow==12.2.0
flask==3.1.3
pdfplumber==0.11.9
# Optional — only for the Anthropic NLI backend:
# anthropic==0.122.0
